#This yml file build the java-maven project, docker image, and push into the docker registry.
name: Maven Build and Docker Push

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  workflow_dispatch:   # allows manual trigger

jobs:
  compile:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout code
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Set up JDK for Maven
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      # 3️⃣ Cache Maven dependencies (optional but faster)
      - name: Cache Maven packages
        uses: actions/cache@v4
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2

      # 4️⃣ Build with Maven
      - name: Build with Maven
        run: mvn -B clean package --file pom.xml
  # security-check:
  #   runs-on: ubuntu-latest
  #   needs: compile

  #   steps:
  #   - uses: actions/checkout@v4
  #   - name: Trivy Installation
  #     run: |
  #       sudo apt-get install -y wget apt-transport-https gnupg lsb-release
  #       wget -qO - https://aquasecurity.github.io/trivy-repo/deb/public.key | sudo apt-key add -
  #       echo deb https://aquasecurity.github.io/trivy-repo/deb $(lsb_release -sc) main | sudo tee -a /etc/apt/sources.list.d/trivy.list
  #       sudo apt-get update -y
  #       sudo apt-get install -y trivy

  #   - name: Trivy FS Scan
  #     run: trivy fs --format table -o fs-report.json .

  #   - name: Gitleaks Installation
  #     run: sudo apt install gitleaks -y
  #   - name: Gitleaks Code Scan
  #     run: gitleaks detect source . -r gitleaks-report.json -f json 

  upload_artifacts:
    runs-on: ubuntu-latest
    needs: security-check
    steps:
       #Upload artifact
    - name: Upload JAR artifact
      uses: actions/upload-artifact@v4
      with:
         name: app-jar
         path: target/*.jar
        
      # 🧩 Fix: Move the built JAR to the correct location for Docker
      # - name: Move JAR for Docker build
      #   run: |
      #       echo "Searching for built JAR..."
      #       JAR_PATH=$(find . -path "*/target/*.jar" -type f | head -n 1)
      #       echo "Found JAR: $JAR_PATH"
      #       mkdir -p target
      #       cp "$JAR_PATH" target/app.jar
  download_artifacts:
     runs-on: ubuntu-latest
     needs: upload_artifacts
     steps:
      #Download artifact
      - name: Download JAR artifact
        uses: actions/download-artifact@v4
        with:
         name: app-jar
         path: app  # this will download JAR to ./app folder
      # 5️⃣ Log in to Docker registry
      # Set your secrets in: Settings → Secrets and variables → Actions
  dockerize:
     runs-on: ubuntu-latest
     needs: download_artifacts
     steps:    
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
         username: ${{ vars.DOCKERHUB_USERNAME }}
         password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build and Push Docker image
        uses: docker/build-push-action@v6
        with:
         context: .
         push: true
         tags: nagendrabathini/docker_images:0.3
         file: ./Dockerfile
      #   # 7️⃣ Debugging: Print push info
      # - name: Debug - Print Docker push details
      #   run: |
      #     echo "✅ Docker image push completed!"
      #     echo "Pushed image tags:"
      #     echo " - ${{ secrets.DOCKER_USERNAME }}/docker_images:0.2"
      #     echo " - ${{ secrets.DOCKER_USERNAME }}/docker_images:${{ github.run_number }}"
      #     echo ""
      #     echo "🔍 Checking local Docker images:"
      #     docker images | grep docker_images || echo "No local images found"
      #     echo ""
      #     echo "🔍 Pulling image from Docker Hub to verify it exists:"
      #     docker pull ${{ secrets.DOCKER_USERNAME }}/docker_images:0.2
      #     echo "✅ Verification completed successfully!"

      
# (Optional) Verify image
#       - name: Verify image
#         run: docker pull ${{ secrets.DOCKER_USERNAME }}/myapp:latest
